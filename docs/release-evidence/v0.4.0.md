# Release Evidence v0.4.0

Date: unreleased

Status: in development. This file is the evidence baseline for the next production portfolio release.

## Scope

v0.4.0 covers:

- v0.3 release-state reconciliation,
- v0.4 development version alignment,
- GitHub publication checklist evidence,
- Dependabot triage evidence,
- runtime preflight and full-stack proof workflow,
- sanitized runtime evidence pack.

## Verification Checklist

- [x] `.\scripts\version-consistency.ps1`
- [x] `.\scripts\docs-quality.ps1`
- [x] `.\scripts\evidence-audit.ps1`
- [x] `.\scripts\repo-hygiene.ps1`
- [x] `.\scripts\portfolio-verify.ps1 -Docs -Docker`
- [x] `mvn -T1 clean verify`
- [x] `cd frontend && npm run typecheck && npm run build`
- [x] `.\scripts\runtime-preflight.ps1`
- [x] `docker compose up -d --build`
- [x] `.\scripts\portfolio-verify.ps1 -Runtime -GatewayUrl http://localhost:8080`
- [x] `.\scripts\runtime-evidence-summary.ps1`

## Runtime Evidence

Runtime evidence is collected only when Docker Desktop is available. Raw generated reports remain ignored under `reports/`; committed evidence must use sanitized summaries only.

Current runtime evidence document:

- `docs/runtime-evidence-v0.4.md`

## Local Baseline Results

| Gate | Status | Notes |
|---|---|---|
| Version consistency | Passed | Maven `0.4.0-SNAPSHOT`, frontend `0.4.0`, latest release `v0.3.0` |
| Docs quality | Passed | v0.4 release docs, runtime evidence, and dependency triage are required paths |
| Evidence audit | Passed | 22 portfolio documentation evidence paths present |
| Repository hygiene | Passed | 0 forbidden tracked artifacts, 0 missing ignore patterns |
| Docker compose config | Passed | Verified by `portfolio-verify.ps1 -Docs -Docker` |
| Backend build | Passed | `mvn -T1 clean verify` after opening v0.4 cycle |
| Frontend build | Passed | `npm run typecheck` and `npm run build` after opening v0.4 cycle |

## Release Readiness

- [ ] GitHub Actions green for the final v0.4 commit.
- [x] Release notes baseline reviewed.
- [x] Release evidence baseline reviewed.
- [ ] Tag `v0.4.0` only after `master` is green.
