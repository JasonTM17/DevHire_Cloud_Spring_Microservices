# DevHire Cloud v1.0.0 Draft Release Notes

Status: draft acceptance plan

These notes describe the target shape for a future `v1.0.0` release. They are intentionally not a published release claim. The latest public release is `v0.6.0`.

## Release Theme

`v1.0.0` should package DevHire Cloud as a complete production engineering portfolio: backend microservices, frontend product flows, deterministic demo data, runtime observability, cloud blueprint verification, security evidence, and trilingual documentation.

## Target Highlights

- Java 21 and Spring Boot microservices with API Gateway, auth, user, company, job, application, notification, audit, AI, and common modules.
- Service-owned PostgreSQL databases with Flyway migrations and deterministic portfolio seed data.
- Kafka eventing with transactional outbox, retry/dead-letter posture, and idempotent consumers.
- OpenSearch search path with PostgreSQL fallback.
- Claude Haiku AI assistant with cited answers, fallback mode, tool traces, safety docs, and eval scripts.
- Next.js reviewer UI with jobs, job detail, candidate, employer, admin, and assistant surfaces.
- Docker Compose full local runtime, Kubernetes raw manifests, Helm, Argo CD, External Secrets, and AWS Terraform blueprint.
- Prometheus, Grafana, Loki, Tempo, OpenTelemetry, Mailpit, SLO docs, and domain metrics.
- GitHub Actions for CI, Docker images, docs, security, CodeQL, E2E, Terraform, and governance.
- Trilingual documentation in Vietnamese, English, and Japanese.

## Acceptance Gates

```powershell
.\scripts\v1-release-verify.ps1 -Cloud
.\scripts\v1-release-verify.ps1 -Backend -Frontend -Cloud
.\scripts\v1-cloud-evidence.ps1
.\scripts\v1-demo-data-verify.ps1
```

Runtime evidence when Docker is running:

```powershell
.\scripts\v1-runtime-evidence.ps1 -GatewayUrl http://localhost:8080
```

## Honest Scope

`v1.0.0` will still be a production-grade portfolio release, not a live customer SaaS claim. AWS remains apply-ready blueprint evidence unless a separate credentialed cloud deployment phase is approved.
