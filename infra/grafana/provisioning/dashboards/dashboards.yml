apiVersion: 1

providers:
  - name: DevHire
    orgId: 1
    folder: DevHire Cloud
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

