name: Release Images

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      image_tag:
        description: Image tag to publish
        required: true
        default: manual

permissions:
  contents: write
  packages: write
  attestations: write
  id-token: write

jobs:
  publish:
    name: Publish ${{ matrix.service }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        service:
          - api-gateway
          - auth-service
          - user-service
          - company-service
          - job-service
          - application-service
          - notification-service
          - audit-service
          - ai-service
          - frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute image tags
        shell: bash
        run: |
          owner="${GITHUB_REPOSITORY_OWNER,,}"
          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            version="${{ inputs.image_tag }}"
          else
            version="${GITHUB_REF_NAME}"
          fi
          {
            echo "IMAGE_NAME=ghcr.io/${owner}/devhire/${{ matrix.service }}"
            echo "VERSION_TAG=${version}"
            echo "BUILD_CREATED=$(date -u +'%Y-%m-%dT%H:%M:%SZ')"
            echo "VCS_URL=${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}"
          } >> "${GITHUB_ENV}"

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.service }}/Dockerfile
          build-args: |
            SERVICE_NAME=${{ matrix.service }}
            BUILD_VERSION=${{ env.VERSION_TAG }}
            VCS_REF=${{ github.sha }}
            VCS_URL=${{ env.VCS_URL }}
            BUILD_CREATED=${{ env.BUILD_CREATED }}
            NEXT_PUBLIC_API_BASE_URL=${{ vars.NEXT_PUBLIC_API_BASE_URL || 'https://devhire.invalid' }}
          tags: |
            ${{ env.IMAGE_NAME }}:${{ env.VERSION_TAG }}
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          labels: |
            org.opencontainers.image.title=devhire-${{ matrix.service }}
            org.opencontainers.image.description=DevHire Cloud ${{ matrix.service }} image
            org.opencontainers.image.source=${{ env.VCS_URL }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.version=${{ env.VERSION_TAG }}
            org.opencontainers.image.created=${{ env.BUILD_CREATED }}
            org.opencontainers.image.licenses=MIT
          provenance: mode=max
          sbom: true
          push: true

  github-release:
    name: Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/')
    needs: publish
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create release from notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: bash
        run: |
          notes_file="docs/release-notes/${GITHUB_REF_NAME}.md"
          if [[ ! -f "${notes_file}" ]]; then
            echo "::error::Missing release notes file: ${notes_file}"
            exit 1
          fi

          gh release create "${GITHUB_REF_NAME}" \
            --title "DevHire Cloud ${GITHUB_REF_NAME}" \
            --notes-file "${notes_file}" \
            --verify-tag
